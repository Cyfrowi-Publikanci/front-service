version: 2.1

jobs:
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: yarn install
          command: yarn install
      - run:
          name: test
          command: yarn test
  build_and_push:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Build and push Docker image
          command: |
            docker build -t kcc112/front-service:latest .
            echo $DOCKER_PWD | docker login -u $DOCKER_LOGIN --password-stdin
            docker push ariv3ra/$IMAGE_NAME:$TAG

workflows:
  base:
    jobs:
      - test:
          filters:
            branches:
              only:
                - ci
      - build_and_push:
          filters:
              branches:
                only:
                  - ci

